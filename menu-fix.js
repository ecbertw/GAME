/* EIXO legacy menu compatibility shim.
   ui.js is the single source of truth for the player submenu.
   This file must never rebuild or reorder #playerMenu. */
(function(){
  // Intentionally left structure-neutral.
})();
