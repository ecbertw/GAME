/* EIXO: before registration, the country picker is always presented in English. */
(function(){
 function apply(){let p=null;try{p=JSON.parse(localStorage.getItem('eixo_player')||'null')}catch(_){};if(p)return;document.documentElement.lang='en';document.documentElement.dir='ltr';const set=(id,v)=>{const e=document.getElementById(id);if(e)e.textContent=v};set('onboardingTitle','WELCOME TO EIXO');set('onboardingText','First choose your country.');const l=document.querySelector('label[for="countrySelect"]');if(l)l.textContent='COUNTRY';set('countryContinue','CONTINUE ▶');const names=new Intl.DisplayNames(['en'],{type:'region'});const label=code=>{let n='';try{n=(names.of(code)||code).toUpperCase()}catch(_){n=code}const f=[...code].map(c=>String.fromCodePoint(127397+c.charCodeAt())).join('');return `${f} ${n}`};const select=document.getElementById('countrySelect');if(select&&window.countryCodes){select.innerHTML=window.countryCodes.map(code=>`<option value="${code}">${label(code)}</option>`).join('');select.value='PT';}const menu=document.getElementById('countryMenu');if(menu&&window.countryCodes){menu.innerHTML=window.countryCodes.map(code=>`<button class="country-option" type="button" data-country="${code}">${label(code)}</button>`).join('');menu.querySelectorAll('.country-option').forEach(btn=>btn.addEventListener('click',()=>window.changeCountry?.(btn.dataset.country)));}}
 apply();
})();
